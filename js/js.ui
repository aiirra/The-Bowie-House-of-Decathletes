const btn = document.querySelector(".animated-btn");
btn.addEventListener("mouseenter",()=>{gsap.to(btn,{scale:1.2,duration:0.3})});
btn.addEventListener("mouseleave",()=>{gsap.to(btn,{scale:1,duration:0.3})});
